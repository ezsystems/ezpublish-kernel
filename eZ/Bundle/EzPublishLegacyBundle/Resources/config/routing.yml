# Internal legacy routes
_ezpublishLegacyTreeMenu:
    pattern: /content/treemenu/{nodeId}/{modified}/{expiry}/{perm}
    defaults:
        _controller: ezpublish_legacy.treemenu.controller:viewMenu
        _format: json

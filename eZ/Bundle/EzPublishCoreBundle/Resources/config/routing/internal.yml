# Internal routes
_ezpublishLocation:
    path: /content/location/{locationId}/{viewType}/{layout}
    defaults:
        _controller: ez_content:viewLocation
        viewType: full
        layout: true

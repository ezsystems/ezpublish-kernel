parameters:
    # Helpers
    ezpublish.content_helper.class: eZ\Publish\Core\Helper\TranslationHelper

services:
    # Helpers
    ezpublish.content_helper:
        class: %ezpublish.content_helper.class%
        arguments: [@ezpublish.config.resolver]

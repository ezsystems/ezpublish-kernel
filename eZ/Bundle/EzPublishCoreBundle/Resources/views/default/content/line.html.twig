{% set content_name=ez_content_name(content) %}

{% if location is defined %}
    <p><a href="{{ path('ez_urlalias', { "locationId": location.id }) }}">{{ content_name }}</a></p>
{% else %}
    <p>{{ content_name }}</p>
{% endif %}

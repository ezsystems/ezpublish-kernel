{{
    render(
        controller(
            "ez_content:viewLocation",
            {
                "locationId": id,
                "viewType": view,
                "params": params
            }
        )
    )
}}

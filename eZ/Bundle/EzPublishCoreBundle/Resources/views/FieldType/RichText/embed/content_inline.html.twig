{{
    render(
        controller(
            "ez_content:viewContent",
            {
                "locationId": id,
                "viewType": view,
                "params": params
            }
        )
    )
}}
